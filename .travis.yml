sudo: false
language: php

php:
  - "hhvm"
  - "5.6"
  - "5.5"
  - "5.4"
  - "5.3"

before_script:
  - git clone --depth=50 --branch=master git://github.com/aimeos/arcavias-core.git ../arcavias-core
  - cd ../arcavias-core && mv ../ai-cache ext/
  - composer require "predis/predis" "0.8.*"

script: vendor/bin/phing -Ddir=ext/ai-cache testext && vendor/bin/phing -Ddir=ext/ai-container checkext
